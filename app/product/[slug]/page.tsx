import { notFound } from 'next/navigation'
import Link from 'next/link'
import { ArrowLeft, Box, Check, ShieldCheck, Truck } from 'lucide-react'
import { getProductBySlug } from '@/lib/catalog'
import { ProductActions } from '@/components/ProductActions'
import { ProductMedia } from '@/components/ProductMedia'
const money=(n:number)=>new Intl.NumberFormat('uk-UA').format(n)+' ₴'
export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=await getProductBySlug(slug);if(!p)notFound();return <main className="product-page shell"><Link href="/catalog" className="back"><ArrowLeft size={16}/>Назад в каталог</Link><div className="product-detail"><div className="detail-image"><ProductMedia src={p.imageUrl} alt={p.name} loading="eager"/><span>{p.season}</span></div><div className="detail-copy"><small className="sku">Артикул {p.sku}</small><h1>{p.name}</h1><p className="lead">Оптовая упаковка с готовым размерным рядом. Фактическое наличие подтверждается менеджером перед оплатой.</p><div className="detail-price"><div><strong>{money(p.unitPrice)}</strong><span>за пару</span></div><div><strong>{money(p.unitPrice*p.packSize)}</strong><span>за весь ящик</span></div></div><div className="detail-grid"><div><span>В ящике</span><b>{p.packSize} пар</b></div><div><span>Размерный ряд</span><b>{p.sizeGrid}</b></div><div><span>Сезон</span><b>{p.season}</b></div><div><span>Материал</span><b>{p.material}</b></div></div><ProductActions product={p}/><div className="detail-trust"><span><ShieldCheck/>Проверяем наличие</span><span><Truck/>Доставка по Украине</span><span><Box/>Опт ящиками</span></div><ul className="clean-list"><li><Check/>Менеджер подтверждает заказ до предоплаты</li><li><Check/>Количество ящиков можно изменить в корзине</li><li><Check/>Цена упаковки рассчитывается автоматически</li></ul></div></div></main>}
